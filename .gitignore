# Dependencies & build
node_modules/
dist/

# OS / editor
.DS_Store

# Cursor / Claude — local IDE state (rules in .cursor/rules/ are committed)
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.claude/

# Secrets & local env (never commit real keys)
.env
.env.*
!.env.example
src/config/authProviders.local.js
