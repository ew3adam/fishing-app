# Dependencies & build
node_modules/
dist/

# OS / editor
.DS_Store

# Cursor / Claude — local IDE state (rules/agents/hooks below are committed on purpose, shared team config)
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/settings.json

# Secrets & local env (never commit real keys)
.env
.env.*
!.env.example
src/config/authProviders.local.js
