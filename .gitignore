# Dependencies & build
node_modules/
dist/

# OS / editor
.DS_Store

# Cursor / Claude — local AI rules & IDE state (keep off public repos)
.cursor/
.claude/

# Secrets & local env (never commit real keys)
.env
.env.*
!.env.example
data/contacts.enc*
data/*.decrypted.csv
