{
  "name": "fishing-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "scan:pii": "node scripts/scan-pii.js --all",
    "scan:pii:staged": "node scripts/scan-pii.js --staged",
    "prepare": "node scripts/setup-git-hooks.js",
    "deploy": "node scripts/scan-pii.js --paths src,public,data && npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "firebase": "^12.14.0",
    "leaflet": "^1.9.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.1.1",
    "vite": "^5.0.0"
  }
}
